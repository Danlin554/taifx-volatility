# 2026-05-20 Session 紀錄

## 完成事項

### HF-2：US partial bar 同步 trim（已 push，線上部署）
- **根因**：HF-1 fallback 只 trim TXF，但 yfinance 在 NYSE 盤中（ET 10:16）也回了 US 當日 partial bar，導致第二次 `_validate_live_raw` 因 `us_last=5/20 != expected_us=5/19` 再次失敗，503 持續。
- **修正**：fallback block 內在 trim TXF 後，同步用 `df.index.date <= us_trim_target` 過濾各 US series；`us_trim_target = expected_us_session_for_effective_date(new_effective)` 不可用時保守用 `new_effective`。
- **commit**：HF-2 已 push，Zeabur 部署後 log 確認 `startup refresh succeeded effective_date=2026-05-19 us_session_date=2026-05-19`。

### UI 重設計：白底淺色主題（已 push，線上部署）
- **問題**：前一版深色（TradingView Dark）被 Dan 反映「都好醜」。
- **決策**：改回白底淺色卡片，保留深色 header（深海軍藍漸層）作視覺錨點。
- **主要設計改動**：
  - 白底卡片 + `#f0f2f5` 背景
  - 所有數字欄位加 `font-variant-numeric: tabular-nums`
  - 語意色 token（藍/綠/紅/黃各區塊）
  - 多方（綠）/ 空方（紅）目標列，以預估開盤 ± 振幅即時計算
  - 日期切換：AbortController + latest-wins token，非 2xx 時 restore date input
- **commit**：`42795b5` — `redesign: light theme with dark header, tabular numbers, long/short targets`

## 未完成事項（下次繼續）

### Batch 2 剩餘功能
- **Readonly + 編輯模式 toggle**：預設欄位 readonly，頁首加「✏️ 手動調整」按鈕切換
- **週別月份 chip 切換**：1月/2月/3月/6月 chip，從 `snapshot.weekday_multi[mN]` 更新
- **`weekday_multi` 後端欄位**：`compute.weekday_avg_multi()` 尚未實作，前端目前只用 `weekday`（6個月固定值）

### Batch 1 後端剩餘
- `read_db_data` / `build_snapshot` 的完整重構（v19 plan）尚未實作，目前線上仍是舊版
- `GET /api/snapshot?asof=` 端點：若後端尚未新增，日期切換會 404

## 關鍵決策紀錄

| 決策 | 結論 |
|------|------|
| UI 風格 | 白底淺色，深色 header，不用 TradingView Dark |
| HF-2 fallback 策略 | TXF + US 同步 trim，trim 後 US < 2 rows 直接 propagate |
| partial-session leakage fallback | 只允許一次，第二次失敗 propagate，不無限重試 |
| 日期切換防競態 | AbortController + latest-wins token（非 `if inflight return`） |
