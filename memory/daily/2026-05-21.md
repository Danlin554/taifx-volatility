# 2026-05-21 Session 紀錄

## 完成事項

### B5 計畫定稿（待批准實作）

使用者反映「光看前日台指期收盤這個數字就是異常的」，啟動商品顯示 + 資料揭露計畫。

**探索發現（關鍵）**：
- `src/fetch.py:_fetch_txf_fubon`（**主資料源**）**完全沒有 session 過濾**（fetch.py:90-94），volume 還硬寫 0。富邦 SDK 若預設回「全時段合併 OHLC」→ high/low/close 都會被夜盤拉寬。**這就是台指收盤異常最可能的根因**。
- FinMind 備援路徑明確過濾 `trading_session == "position"`（fetch.py:147）→ 純日盤，安全。
- `TXFR1` = 大臺近月 ✅（不是小台 MXFR1）。
- `prev_close = float(txf["close"].iloc[-1])`（main.py:446）：是 `iloc[-1]` 不是 `iloc[-2]`，命名「prev」是相對於下一個 TW 開盤而言（用來預測下一日開盤），**不是 off-by-one bug**。
- DB schema `tvol_indices_daily` 已有 `open_px/high_px/low_px/close_px`，OHL 不需新增欄位即可顯示。
- snapshot 內 `us` 目前只有 pct（main.py:445）、沒有原始 close。
- 前端「前日台指收盤」只有單一 close input、無日期、無 OHL（templates/index.html.j2:273-274）。美股表頭只有「指數 / 漲幅(%) / 權重(%)」、無 close、無 NYSE 收盤日揭露（line 247）。

**計畫定稿內容**（plan file：`/home/dan/.claude/plans/1-2-reactive-hamster.md`，副本 `docs/plans/2026-05-21_b5-product-display.md`）：

| 區塊 | 動作 |
|------|------|
| B5.1 富邦日盤過濾 | 加 `FORCE_FINMIND_TXF` 旗標當保險閘 + 寫 `src/verify_sources.py` diff 兩來源 + 研究富邦 SDK session 參數 |
| B5.2 後端 schema | snapshot 加 `txf_ohlc{date,o,h,l,c}`、`txf_symbol_label`、`us_data{<k>:{date,close,pct}}`（additive） |
| B5.3 前端顯示 | 前日台指卡片改 OHLC readonly + 商品標籤 + 日期；美股表頭 `商品/收盤/漲幅/權重`，加 NYSE 收盤日 meta badge；商品命名格式「DJ（道瓊工業）」 |
| E1 漲幅精度 | `toFixed(2)` 統一前後端 |

**使用者決策回答**：
- 富邦日盤過濾：這次一起做（推薦）
- 商品命名：英文 + 中文括號
- 美股 close：加 close 欄位
- OHLC 編輯：全部 readonly

## 未完成事項（下次繼續）

### 立即優先（plan 已定稿，待 ExitPlanMode 批准）
- B5 全部實作（fetch / schema / template / patchDOM）
- Zeabur 線上設 `FORCE_FINMIND_TXF=1` 立即止血

### 仍待後續批次
- C1 Readonly + 編輯模式 toggle
- C2 週別月份 chip 切換（後端 `weekday_avg_multi` 已實作，只缺前端）
- D1 `GET /api/snapshot?asof=` 端點實作（日期切換目前會 404）
- D2 `read_db_data` / `build_snapshot` v19 三層分離重構
- F1 NASDAQ symbol 確認（`^IXIC` 綜合 vs `^NDX` 100 vs `NQ=F` 期貨）— 使用者明說晚點處理
- F2 富邦 SDK 永久解法（B5.1 步驟 3）
- G1/G2 多方/空方卡片 5 組 + 整頁風格 10 組 mockup

## 關鍵決策紀錄

| 決策 | 結論 |
|------|------|
| 台指收盤異常根因 | 富邦 fetch 缺日盤過濾（高機率） |
| 商品命名 | 英文 + 中文括號，例如 `DJ（道瓊工業）` |
| OHLC 顯示 | 前日台指期 OHL+C 全 readonly |
| NQ symbol 處理 | 本批次保持 `^IXIC`（那斯達克綜合），晚點再決定 |
| 富邦修復策略 | 先加 `FORCE_FINMIND_TXF` 保險閘 + verify script，永久解法看 SDK 文件研究結果 |
